include(FindPackageHandleStandardArgs)
set(${CMAKE_FIND_PACKAGE_NAME}_CONFIG ${CMAKE_CURRENT_LIST_FILE})
find_package_handle_standard_args(@TINY_EXR_TARGET@ CONFIG_MODE)

if(NOT TARGET @TINY_EXR_TARGET@)
    if (@tinyEXR_USES_ZLIB@)
        find_dependency(ZLIB REQUIRED)
    endif()
    include("${CMAKE_CURRENT_LIST_DIR}/@TINY_EXR_TARGET@Targets.cmake")
endif()
